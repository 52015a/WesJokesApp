@model IEnumerable<WesJokesApp.Models.Jokes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Criar Nova Anedota</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.JokePergunta) *@
                Pergunta-me por: 
            </th>
          
            <th></th>
            <th>Colocado por: </th>
        </tr>

    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.JokePergunta)
            </td>
                <td> @Html.DisplayFor(modelItem => item.IdUser)</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.id">Detailhes</a> |
                <a asp-action="Delete" asp-route-id="@item.id">~Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
